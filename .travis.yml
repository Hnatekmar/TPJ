language: cpp

sudo: required

compiler:
        - g++
        - clang

os:
        - linux
        - osx

addons:
        apt:
                sources:
                        - sourceline: 'ppa:george-edison55/cmake-3.x'

                packages:
                        - libboost-all-dev
                        - cmake

script:
        - cd project/
        - cmake CMakeLists.txt
        - make
