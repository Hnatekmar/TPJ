#ifndef EVALUATOR_H
#define EVALUATOR_H

#include <iostream>
#include <map>
#include <functional>
#include <stack>
#include "AST.h"

class Evaluator
{
	//const AST& m_ast;
	//std::ostream& m_stream;
	public:
	//	Evaluator(const AST& ast, std::ostream stream);
		void run();
};

#endif
